{
  "description": "ac plugin hooks: nudge Claude to invoke the user's personal twin skills at session start, inject autonomous-run resume reminder when an .execution-state.md is present, and preserve plan state through compaction via PreCompact.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/session-start.mjs",
            "timeout": 3
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/pre-compact.mjs",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
